import { makeStyles } from '@material-ui/core/styles'

export const useTableStyles = makeStyles({
  tableContainer: {
    marginTop: 20,
    MaxWidth: '90vw'
  },
  colorBox: {
    width: 20,
    height: 20,
    margin: 'auto'
  }
}
)
